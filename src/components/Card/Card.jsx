import React from "react"

export default function Card(){
    return (
        <div className="card">
            {/* icon from props? */}
            {/* title from props */}
            {/* description from children */}
        </div>
    )
}